{
  "name": "e2ee-test",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "wasm": "node scripts/wasm.mjs",
    "wasm:build": "E2EE_PRECOMPILED_WASM=0 node scripts/wasm.mjs",
    "dev": "vite --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run src/e2eeClient.test.ts",
    "test:live": "vitest run src/e2eeLive.test.ts",
    "wasm:precompiled": "E2EE_PRECOMPILED_WASM=1 node scripts/wasm.mjs",
    "check": "npm run wasm && npm run typecheck && npm test",
    "check:precompiled": "npm run wasm:precompiled && npm run typecheck && npm test",
    "build": "npm run wasm && npm run typecheck && vite build",
    "build:precompiled": "E2EE_PRECOMPILED_WASM=1 npm run build",
    "ci": "npm run check:precompiled && npm run build:precompiled && npm run check && npm run build && npm audit --audit-level=high",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "vite": "^8.0.13"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "typescript": "^5.9.3",
    "vitest": "^4.1.6",
    "wasm-pack": "^0.15.0"
  }
}
